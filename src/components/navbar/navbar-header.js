import React from "react"
import { StyleSheet, View } from "react-native";

export const NavbarHeader =({language, exist})=>{

   return (
      <View>

      </View>
   )
};

const styles = StyleSheet.create({

})